name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  ci:
    strategy:
      matrix:
        rust: [stable]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt
      - run: cargo fmt -- --check
      - name: Cargo Test
        env:
          RUST_BACKTRACE: 1
          RUST_LOG: debug
        run: cargo test --workspace --exclude smallsorts --exclude analytic-engine --exclude analytic-parser
      - run: cargo build
