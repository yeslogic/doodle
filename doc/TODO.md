# TODO

- [ ] Go back in `otf_metrics` and handle undecorated-CommonObj in Container traits that ought to be Mandatory/Nullable (and explicitly mark which ones should be neither)
- [ ] Review whether `Link = Option` alias is still necessary/useful at this stage
