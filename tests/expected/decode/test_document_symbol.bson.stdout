└── document <- bson.document :=
    └── elements <- repeat bson.element :=
        └── 0 <- bson.element :=
            └── symbol <- bson.element.symbol :=
                ├── key <- bson.cstring := { valid := "sym" }
                └── value <- bson.string := { valid := "sym" }
